# Redis 持久化配置（Windows 本地开发用）
#
# 使用方式：redis-server redis.conf

# RDB 文件保存目录
dir D:/code/26autumn-pracrice-training/cs-visual-learn/ai-service

# 禁用自动 BGSAVE（Windows 不支持 fork），改用应用层手动 SAVE
save ""

# BGSAVE 失败时不拒绝写入
stop-writes-on-bgsave-error no
